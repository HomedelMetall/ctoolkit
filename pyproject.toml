[build-system]
requires = ["numpy", "scipy", "matplotlib", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[tool.autopub]
project-name = "ctoolkit"
git-username = "cescorihuela"
git-email = "c.escorihuela.sayalero@gmail.com"
version-strings = "0.0.1"

[tool.setuptools_scm]
